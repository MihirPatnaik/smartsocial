// src/smartsocial/agents/helpers/claudePromptWrapper.ts
export const claudePromptImageWrapper = (caption: string) => {
  return `Generate a highly detailed, creative image for the following description: ${caption}`;
};
